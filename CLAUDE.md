# 중요 규칙

**언어**: 항상 한글로 대답한다.

**실행 금지**: 절대로 코드를 실행하지 마라. 코드 수정만 하고, 실행은 사용자가 직접 한다.

**Git Commit**: 에러가 해결되거나 새로운 기능이 추가되는 등 의미 있는 변경사항이 완료되면 적절한 타이밍에 git commit을 실행한다.
- Commit 메시지는 한글로 작성하며 변경 내용을 명확하게 설명해야 한다.
- Commit 시 불필요한 파일(untracked 파일, 백업 파일, 임시 파일 등)은 포함하지 않는다.
- 수정된 파일(modified)만 선별하여 커밋한다.
- 필요한 경우 사용자에게 untracked 파일 중 커밋할 만한 것이 있는지 물어본다.
